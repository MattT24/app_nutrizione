package it.nutrizionista.restnutrizionista.enums;

/** Stato calcolato di un account demo. La scadenza resta sempre determinata server-side. */
public enum StatoAccountDemo {
    ATTIVO,
    SCADUTO,
    DISABILITATO
}
